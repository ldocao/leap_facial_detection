n_points = 200 #number of points to classify
max_epochs = 20000 #maximum number of epochs to train the data
evaluate_loss_frequency = 5 #evaluate loss every N epochs
visualize_classifier_frequency = 5 #visualize classifier every N epoch
stop_criterion = 0.1 #if loss < stop_criterio, then stop training
epsilon = 0.01 # learning rate for gradient descent